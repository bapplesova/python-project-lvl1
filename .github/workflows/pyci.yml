name: lint
on: push
jobs:
        my-job:
                runs-on: ubuntu-latest
                steps:
                        - name: Run linter
                          run: poetry run flake8 brain_games
